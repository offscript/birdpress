<!--
This is the header partial. Different headers are displayed when the user is logged in than when the user is logged out.
-->
<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "BirdPress", root_path, id: "logo" %>
    <nav>
      <div>
        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to "Home", root_url %></li> <!-- Use of the link_to helper method, which is used to create <a>
          <% if logged_in? %> <!-- We use the custom logged_in? function found in app/helpers/sessions_helper.rb to determine if the user is logged in. If the function evaluates to true we provide links to create a post, look at users, visit his account etc. -->
          <li><%= link_to "Write", new_micropost_path %></li>
          <li><%= link_to "Users", users_path %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", current_user %></li>
              <li><%= link_to "Settings", edit_user_path(current_user) %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Log out", logout_path, method: "delete" %> <!-- Here we use a modification of link_to helper to log out a logged in User. This link_to will send a delete request the logout_path which is the equivalent to birdpress.com/logout. As you can see in app/config/routes.rb this delete request corresponds to the destroy action in the sessions controller at app/controller/sessions_controller.rb -->
              </li>
            </ul>
          </li>
          <% else %>
            <li><%= link_to "About",  about_path %></li>
            <li><%= link_to "Sign Up", new_user_path %></li>
            <li><%= link_to "Log in", login_path %></li>
        <% end %>
        </ul>
        </div>
    </nav>
  </div>
</header>